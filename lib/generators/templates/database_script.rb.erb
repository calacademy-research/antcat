module DatabaseScripts
  class <%= class_name %> < DatabaseScript
    def results
      # Subspecies.where(species: nil)
    end

    # When this method isn't implemented, the code tries to figure out
    # how to render `#results` based on its class.
    # def render
    #   as_table do |t|
    #     t.header :taxon, :status
    #     t.rows do |taxon|
    #       [
    #         markdown_taxon_link(taxon),
    #         taxon.status
    #       ]
    #     end
    #   end
    # end
  end
end

__END__

# title: Defaults to `DatabaseScript#filename_without_extension.humanize`.
category: Catalog, References... see the script index page
tags: [new!, example-tag]

issue_description: Optional short description.

description: >
  Optional script description; parsed as **markdown**.

  Tags are optional (some have special meanings, see `DatabaseScript`).

related_scripts:
  - TaxaWithSameName
  - OptionalClassNamesOfOtherScript
