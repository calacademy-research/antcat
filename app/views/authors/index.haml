- @title = 'Authors'

%div= will_paginate @authors

#authors
  - for author in @authors
    .author_row
      %span= author.names.map(&:name).join('; ')
      - if user_can_edit?
        = link_to 'edit', edit_author_path(author), class: 'edit_link'
      %br

%div= will_paginate @authors
