=form_with model: tooltip do |f|
  =render 'shared/errors_for', resource: tooltip
  .row.mb-4
    .medium-6.columns
      =f.label :key
      =f.text_field :key

      .mb-4

      =f.label :scope
      =f.text_field :scope

  .row.mb-4
    .medium-6.columns
      =f.label :text do
        Text
        =tooltip_icon "HTML is allowed, but double quotes are not 100% supported."
      =f.text_area :text, cols: 40, rows: 5

  .row.mb-4
    .medium-6.columns
      =edit_summary_text_field_tag

  .row
    .medium-6.columns
      .actions
        =f.button 'Save', class: "btn-saves"
