%tr
  %td
    =link_to tooltip.key, tooltip_path(tooltip)
  %td
    =tooltip.enabled ? "yes" : "no"
  %td
    =tooltip.text
  %td
    =tooltip.selector
  %td
    =tooltip.selector_enabled ? "yes" : "no"