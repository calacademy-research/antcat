=format_submitter_name feedback
submitted (#{time_ago_in_words(feedback.created_at)} ago):

.callout{class: ("gray" if feedback.closed?)}
  =format_feedback_page feedback
  =simple_format strip_tags(feedback.comment)
