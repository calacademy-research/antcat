-# currently only :create

-user_id = activity.parameters[:user_id]
-if User.exists? user_id
  =User.find(user_id).decorate.name_linking_to_email
-else
  [deleted user]
registered an account, welcome to antcat.org!
