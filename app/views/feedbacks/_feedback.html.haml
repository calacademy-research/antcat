%h6
  =feedback.decorate.format_submitter_name
  submitted (#{time_ago_in_words(feedback.created_at)} ago)

%p{class: ("text-gray-600" if feedback.closed?)}
  =feedback.decorate.format_page
  =simple_format strip_tags(feedback.comment)

%br
