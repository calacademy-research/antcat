- @title = 'Users'

%table
  %tr
    %th
    %th Name
    %th Email
  - for user in @users
    %tr
      %td= user.can_edit_catalog? ? '*' : ' '
      %td= format_name_linking_to_email user.name, user.email
      %td= user.email
* = editor
