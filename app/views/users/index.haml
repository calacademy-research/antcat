-@title = 'Users'
-breadcrumb :users

%table
  %thead
    %th Name
    %th Editor
    %th Email
  -@users.each do |user|
    %tr
      %td=user.decorate.user_page_link
      %td=user.can?(:edit, :catalog) ? 'Yes' : dash
      %td=user.email
