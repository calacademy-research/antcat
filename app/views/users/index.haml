- @title = 'Users'

%table
  %tr
    %th ID
    %th
    %th Name
    %th Email
  - for user in @users
    %tr
      %td.id= user.id
      %td= user.can_edit? ? '*' : ' '
      %td= user.decorate.format_name_linking_to_email
      %td= user.email
* = editor
