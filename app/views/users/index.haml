-@title = 'Users'
-breadcrumb :users

-if user_is_superadmin?
  .buttons-right
    =link_to "New (for superadmins)", new_user_path, class: "btn-normal"

%table
  %thead
    %th Name
    %th
    %th Email
  -@users.each do |user|
    %tr
      %td=user.decorate.user_page_link
      %td=user.decorate.user_badge
      %td=user.email
