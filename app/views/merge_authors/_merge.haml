.callout
  =form_tag merge_authors_merge_path do
    -panels.each do |panel|
      =hidden_field_tag 'terms[]', panel.term
    %p.center-text
      =button_tag 'Merge these authors', type: "submit", data: { confirm: 'Do you want to merge these authors?' }, class: "btn-saves-warning"
  .help
    Click this button to make
    =author_names_sentence names
    refer to the same author, so that searching for one of the names finds the others.
