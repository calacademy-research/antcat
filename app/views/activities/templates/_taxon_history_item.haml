=activity.action_to_verb
the history item
=activity.link_trackable_if_exists

-if (protonym_id = activity.parameters[:protonym_id])
  belonging to
  =ActivityDecorator.link_protonym_if_exists protonym_id
-elsif (taxon_id = activity.parameters[:taxon_id])
  belonging to
  =ActivityDecorator.link_taxon_if_exists taxon_id
