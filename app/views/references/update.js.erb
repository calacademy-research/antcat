var id = <%= @reference.id %>;
var html = '<%= escape_javascript(render(:file => "/references/reference")) %>';
$('#reference_' + id).html(html);
$('#reference_display_' + id).show();
$('#reference_form_' + id).hide();
$('#reference_display_' + id).effect("highlight", {}, 3000);
