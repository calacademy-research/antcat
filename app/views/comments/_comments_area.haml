-# Renders form for posting new comment + all current comments.

-content_for :head do
  =javascript_include_tag "markdown_and_friends"

%a{name: "comments"}

%h6.margin-top=pluralize(commentable.comment_threads.count, "comment")
=render partial: "comments/form"

.comments-container.margin-bottom
  =render partial: "comments/comments", locals: { comments: commentable.root_comments }
