-content_for :head do
  =javascript_include_tag "sortable_tables"

%table.stack.tablesorter.hover.margin-top
  %thead
    %tr
      %th.no-wrap Id
      %th.no-wrap Table
      %th.no-wrap Field
      %th.no-wrap Related taxon
      %th.no-wrap Taxt
  %tbody
    -if table_refs.empty?
      %tr
        %td Found no associated items.
    -else
      -table_refs.each do |table_ref|
        -decorated = TableRefDecorator.new(table_ref)
        %tr
          %td=decorated.item_link
          %td
            %code=table_ref.table
          %td
            %code=table_ref.field
          %td=decorated.related_links
          %td.grow.small-text=table_ref.detax
