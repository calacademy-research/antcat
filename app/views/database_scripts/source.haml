-@title = "Show Source - #{@script.title}"
-breadcrumb :show_database_script_source, @script

-content_for :head do
  =stylesheet_link_tag "rouge_syntax_highlight"

=link_to "Back to script", database_script_path(@script), class: "btn-normal"

.callout
  =preserve do
    %pre.highlight
      =highlight_ruby @script.source_code
