- @title = 'Journals'

%table
  %tr
  - for journal in @journals
    %tr
      %td= link_to journal.name, edit_journal_path(journal)

- if user_can_edit?
  %p= button_to "New Journal", new_journal_path, :method => :get
