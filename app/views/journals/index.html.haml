- @title = 'Journals'

%table
  -@journals.each do |journal|
    %tr
      %td
        -if user_can_edit?
          =link_to journal.name, edit_journal_path(journal)
        -else
          =journal.name

-if user_can_edit?
  %p= button_to "New Journal", new_journal_path