.row
  .medium-8.columns
    =form_with model: institution do |f|
      =render "shared/errors_for", resource: institution

      .row
        .medium-4.columns.end
          =f.label :abbreviation
          =f.text_field :abbreviation, maxlength: Institution::ABBREVIATION_MAX_LENGTH
      .row
        .medium-12.columns
          =f.label :name
          =f.text_field :name, maxlength: Institution::NAME_MAX_LENGTH
      .row.margin-top
        .medium-4.columns.end
          =edit_summary_text_field_tag
      .row
        .small-12.columns.end
          =f.button 'Save', class: 'btn-saves'

  .medium-4.columns
    .callout
      %p Institutions are used in some "taxt items". Full names of institutions are shown when abbreviations are hovered.
      %p
        =antcat_icon 'warning-icon'
        %strong Note that there is no validation for checking if an abbreivation is used anywhere.
        Please update related "taxt items" if you change an abbreivation, or make sure it is not being used.
